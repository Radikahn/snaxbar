package snax;

public class Config {
    // Static field to store the configuration
    // true = show on first two slots (0, 1)
    // false = show on last two slots (7, 8)
    public static boolean showOnFirstTwoSlots = true;
}